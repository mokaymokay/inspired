<% @posts.each do |post| %>
  <div class="card my-4">
    <div class="card-body">
      <div class="mb-3"><small><%= post.user.username %></small></div>
      <blockquote class="blockquote mb-0">
        <p><%= post.quote %></p>
        <footer class="blockquote-footer"><%= post.author %></footer>
      </blockquote>
      <br>
      <% post.tags.each do |tag| %>
        <small>#<%= tag.content %></small>
      <% end %>
    </div>
  </div>
<% end %>
